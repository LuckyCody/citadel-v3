Doing expand get, insert and prove unique annotations... No change.
Doing simplify (non-expanded game)... No change.
Doing expand... Done.
Doing remove assignments of findcond... No change.
Doing simplify... Run simplify 1 time(s). Fixpoint reached.
No change.
Doing move all binders... No change.
Doing SA rename new without array accesses and remove assignments of findcond... No change.
Doing merge branches... No change.
Proof of (one-session) secrecy of K failed:
  K is not defined only by restrictions or assignments.
Trying equivalence dhkem_indcca2_assumption... Transf. OK Proba. computed Transf. done Succeeded.
Doing simplify (non-expanded game)... Done.
Doing expand... Done.
Doing remove assignments of findcond... Done.
Doing simplify... Run simplify 2 time(s). Fixpoint reached.
Done.
Doing move all binders... No change.
Doing SA rename new without array accesses and remove assignments of findcond... Done.
Doing merge branches... No change.
Trying equivalence rom(kdf)... Transf. OK Proba. computed Transf. done Succeeded.
Doing simplify (non-expanded game)... No change.
Doing expand... Done.
Doing remove assignments of findcond... Done.
Doing simplify... Doing global dependency analysis on s_1 inside simplify... Done. Restarting simplify
Eliminated collisions between s_1 and x2 Probability: N_kdf / |dh_secret|
(s_1 collides with a value independent of s_1[...] with probability at most 1 / |dh_secret|;
 x2 does not depend on s_1[...])
Run simplify 2 time(s). Fixpoint reached.
Done.
Doing move all binders... Done.
Doing SA rename new without array accesses and remove assignments of findcond... Done.
Doing merge branches... No change.
Doing simplify... Run simplify 1 time(s). Fixpoint reached.
No change.
Proved one-session secrecy of K
Proved secrecy of K
===================== Proof starts =======================
Initial state
Game 1 is
     Ostart() :=
     hk <-R hashkey;
     seed <-R dh_keyseed;
     pk: dh_pkey <- dh_pkgen(seed);
     r <-R dh_encapseed;
  {8}let dh_pair(ss_x25519: dh_secret, ct: dh_ciphertext) = dh_encap_r(pk, r) in
     return(pk, ct);
     ((
       foreach iH <= N_kdf do
       OH(x1: bitstring, x2: dh_secret) :=
       return(kdf(hk, x1, x2))
     ) | (
       foreach id <= N_dec do
       ODecap(c: dh_ciphertext) :=
       if c = ct then
         yield
       else
         return(dh_decap(c, dh_skgen(seed)))
     ) | (
       OChal(ss_mlkem: bitstring) :=
       K: kdfkey <- kdf(hk, ss_mlkem, ss_x25519)
     ))


Applying expand
  - Expand if/find/let
    - Simplify pattern dh_pair(ss_x25519: dh_secret, ct: dh_ciphertext) (bijective tuple expanded) at 8
yields

Game 2 is
     Ostart() :=
     hk <-R hashkey;
     seed <-R dh_keyseed;
     pk: dh_pkey <- dh_pkgen(seed);
     r <-R dh_encapseed;
     v_3: dh_output <- dh_encap_r(pk, r);
     ct: dh_ciphertext <- 2-proj-dh_pair(v_3);
     ss_x25519: dh_secret <- 1-proj-dh_pair(v_3);
     return(pk, ct);
     ((
       foreach iH <= N_kdf do
       OH(x1: bitstring, x2: dh_secret) :=
       return(kdf(hk, x1, x2))
     ) | (
       foreach id <= N_dec do
       ODecap(c: dh_ciphertext) :=
       if c = ct then
         yield
       else
         return(dh_decap(c, dh_skgen(seed)))
     ) | (
       OChal(ss_mlkem: bitstring) :=
       K: kdfkey <- kdf(hk, ss_mlkem, ss_x25519)
     ))


Applying equivalence dhkem_indcca2_assumption [probability P_dh_indcca2(time_1, 1, 1, N_dec)]
  - Equivalence dhkem_indcca2_assumption with variables: r -> r, seed -> seed
yields

Game 3 is
     Ostart() :=
     hk <-R hashkey;
     seed_1 <-R dh_keyseed;
     pk: dh_pkey <- dh_pkgen(seed_1);
     r_1 <-R dh_encapseed;
     s_1 <-R dh_secret;
     v_3: dh_output <- (pk_1: dh_pkey <- pk; if {13}(pk_1 = dh_pkgen(seed_1)) then encoded_i2: encoded_N_keys_t <- encode_idx_seed(); v_4: dh_output <- dh_encap_r(pk_1, r_1); c_1: dh_ciphertext <- 2-proj-dh_pair(v_4); s: dh_secret <- cst_dh_secret; dh_pair(s_1, c_1) else dh_encap_r(pk_1, r_1));
     ct: dh_ciphertext <- 2-proj-dh_pair(v_3);
     ss_x25519: dh_secret <- 1-proj-dh_pair(v_3);
     return(pk, ct);
     ((
       foreach iH <= N_kdf do
       OH(x1: bitstring, x2: dh_secret) :=
       return(kdf(hk, x1, x2))
     ) | (
       foreach id <= N_dec do
       ODecap(c: dh_ciphertext) :=
       if c = ct then
         yield
       else
         return((c_2: dh_ciphertext <- c; if defined(s, c_1, encoded_i2) && (encoded_i2 = encode_idx_seed()) && (c_1 = c_2) then dh_injbot(s_1) else dh_decap(c_2, dh_skgen(seed_1))))
     ) | (
       OChal(ss_mlkem: bitstring) :=
       K: kdfkey <- kdf(hk, ss_mlkem, ss_x25519)
     ))


Applying simplify (non-expanded game)
  - Simplification pass
    - Replaced (pk_1 = dh_pkgen(seed_1)) with true at 13
yields

Game 4 is
     Ostart() :=
     hk <-R hashkey;
     seed_1 <-R dh_keyseed;
     pk: dh_pkey <- dh_pkgen(seed_1);
     r_1 <-R dh_encapseed;
     s_1 <-R dh_secret;
     v_3: dh_output <- (pk_1: dh_pkey <- pk; {12}if true then encoded_i2: encoded_N_keys_t <- encode_idx_seed(); v_4: dh_output <- dh_encap_r(pk_1, r_1); c_1: dh_ciphertext <- 2-proj-dh_pair(v_4); s: dh_secret <- cst_dh_secret; dh_pair(s_1, c_1) else dh_encap_r(pk_1, r_1));
     ct: dh_ciphertext <- 2-proj-dh_pair(v_3);
     ss_x25519: dh_secret <- 1-proj-dh_pair(v_3);
     return(pk, ct);
     ((
       foreach iH <= N_kdf do
       OH(x1: bitstring, x2: dh_secret) :=
       return(kdf(hk, x1, x2))
     ) | (
       foreach id <= N_dec do
       ODecap(c: dh_ciphertext) :=
       if c = ct then
         yield
       else
         return((c_2: dh_ciphertext <- c; {64}if defined(s, c_1, encoded_i2) && (encoded_i2 = encode_idx_seed()) && (c_1 = c_2) then dh_injbot(s_1) else dh_decap(c_2, dh_skgen(seed_1))))
     ) | (
       OChal(ss_mlkem: bitstring) :=
       K: kdfkey <- kdf(hk, ss_mlkem, ss_x25519)
     ))


Applying expand
  - Expand if/find/let
    - Transformed find at 12 into a test
    - Test at 12 always true
    - Remove branch 1 in find at 64
    - Find at 64 removed (else branch kept if any)
yields

Game 5 is
     Ostart() :=
     hk <-R hashkey;
     seed_1 <-R dh_keyseed;
     pk: dh_pkey <- dh_pkgen(seed_1);
     r_1 <-R dh_encapseed;
     s_1 <-R dh_secret;
     pk_1: dh_pkey <- pk;
     encoded_i2: encoded_N_keys_t <- encode_idx_seed();
     v_4: dh_output <- dh_encap_r(pk_1, r_1);
     c_1: dh_ciphertext <- 2-proj-dh_pair(v_4);
     s: dh_secret <- cst_dh_secret;
     v_3: dh_output <- dh_pair(s_1, c_1);
     ct: dh_ciphertext <- 2-proj-dh_pair(v_3);
     ss_x25519: dh_secret <- 1-proj-dh_pair(v_3);
     return(pk, ct);
     ((
       foreach iH <= N_kdf do
       OH(x1: bitstring, x2: dh_secret) :=
       return(kdf(hk, x1, x2))
     ) | (
       foreach id <= N_dec do
       ODecap(c: dh_ciphertext) :=
       if c = ct then
         yield
       else
         c_2: dh_ciphertext <- c;
         return(dh_decap(c_2, dh_skgen(seed_1)))
     ) | (
       OChal(ss_mlkem: bitstring) :=
       K: kdfkey <- kdf(hk, ss_mlkem, ss_x25519)
     ))


Applying remove assignments of findcond
  - Remove assignments on pk_1 (definition removed, all usages removed)
  - Remove assignments on encoded_i2 (definition removed, all usages removed)
  - Remove assignments on s (definition removed, all usages removed)
  - Remove assignments on c_2 (definition removed, all usages removed)
yields

Game 6 is
     Ostart() :=
     hk <-R hashkey;
     seed_1 <-R dh_keyseed;
     pk: dh_pkey <- dh_pkgen(seed_1);
     r_1 <-R dh_encapseed;
     s_1 <-R dh_secret;
     v_4: dh_output <- dh_encap_r(pk, r_1);
     c_1: dh_ciphertext <- 2-proj-dh_pair(v_4);
     v_3: dh_output <- dh_pair(s_1, c_1);
     ct: dh_ciphertext <- {21}2-proj-dh_pair(v_3);
     ss_x25519: dh_secret <- {24}1-proj-dh_pair(v_3);
     return(pk, ct);
     ((
       foreach iH <= N_kdf do
       OH(x1: bitstring, x2: dh_secret) :=
       return(kdf(hk, x1, x2))
     ) | (
       foreach id <= N_dec do
       ODecap(c: dh_ciphertext) :=
       if c = ct then
         yield
       else
         return(dh_decap(c, dh_skgen(seed_1)))
     ) | (
       OChal(ss_mlkem: bitstring) :=
       K: kdfkey <- kdf(hk, ss_mlkem, ss_x25519)
     ))


Applying simplify
  - Simplification pass
    - Replaced 2-proj-dh_pair(v_3) with c_1 at 21
    - Replaced 1-proj-dh_pair(v_3) with s_1 at 24
yields

Game 7 is
     Ostart() :=
     hk <-R hashkey;
     seed_1 <-R dh_keyseed;
     pk: dh_pkey <- dh_pkgen(seed_1);
     r_1 <-R dh_encapseed;
     s_1 <-R dh_secret;
     v_4: dh_output <- dh_encap_r(pk, r_1);
     c_1: dh_ciphertext <- 2-proj-dh_pair(v_4);
     v_3: dh_output <- dh_pair(s_1, c_1);
     ct: dh_ciphertext <- c_1;
     ss_x25519: dh_secret <- s_1;
     return(pk, ct);
     ((
       foreach iH <= N_kdf do
       OH(x1: bitstring, x2: dh_secret) :=
       return(kdf(hk, x1, x2))
     ) | (
       foreach id <= N_dec do
       ODecap(c: dh_ciphertext) :=
       if c = ct then
         yield
       else
         return(dh_decap(c, dh_skgen(seed_1)))
     ) | (
       OChal(ss_mlkem: bitstring) :=
       K: kdfkey <- kdf(hk, ss_mlkem, ss_x25519)
     ))


Applying SA rename new without array accesses and remove assignments of findcond
  - Remove assignments on v_3 (definition removed, all usages removed)
  - Remove assignments on ct (definition removed, all usages removed)
  - Remove assignments on ss_x25519 (definition removed, all usages removed)
yields

Game 8 is
     Ostart() :=
     hk <-R hashkey;
     seed_1 <-R dh_keyseed;
     pk: dh_pkey <- dh_pkgen(seed_1);
     r_1 <-R dh_encapseed;
     s_1 <-R dh_secret;
     v_4: dh_output <- dh_encap_r(pk, r_1);
     c_1: dh_ciphertext <- 2-proj-dh_pair(v_4);
     return(pk, c_1);
     ((
       foreach iH <= N_kdf do
       OH(x1: bitstring, x2: dh_secret) :=
       return(kdf(hk, x1, x2))
     ) | (
       foreach id <= N_dec do
       ODecap(c: dh_ciphertext) :=
       if c = c_1 then
         yield
       else
         return(dh_decap(c, dh_skgen(seed_1)))
     ) | (
       OChal(ss_mlkem: bitstring) :=
       K: kdfkey <- kdf(hk, ss_mlkem, s_1)
     ))


Applying equivalence rom(kdf)
  - Equivalence rom(kdf) with variables: hk -> hk_1
yields

Game 9 is
     Ostart() :=
     seed_1 <-R dh_keyseed;
     pk: dh_pkey <- dh_pkgen(seed_1);
     r_1 <-R dh_encapseed;
     s_1 <-R dh_secret;
     v_4: dh_output <- dh_encap_r(pk, r_1);
     c_1: dh_ciphertext <- 2-proj-dh_pair(v_4);
     return(pk, c_1);
     ((
       foreach iH <= N_kdf do
       OH(x1: bitstring, x2: dh_secret) :=
       return((x_O_1: bitstring <- x1; x_O: dh_secret <- x2; find [unique] u_1 = ri_1 <= N_kdf suchthat defined(x_O_1[ri_1], x_O[ri_1], r_2[ri_1]) && (x_O_1 = x_O_1[ri_1]) && (x_O = x_O[ri_1]) then r_2[u_1] orfind  suchthat defined(x_O_3, x_O_2, r_3) && (x_O_1 = x_O_3) && (x_O = x_O_2) then r_3 else r_2 <-R kdfkey; r_2))
     ) | (
       foreach id <= N_dec do
       ODecap(c: dh_ciphertext) :=
       if c = c_1 then
         yield
       else
         return(dh_decap(c, dh_skgen(seed_1)))
     ) | (
       OChal(ss_mlkem: bitstring) :=
       K: kdfkey <- (x_O_3: bitstring <- ss_mlkem; x_O_2: dh_secret <- s_1; find [unique] u = ri <= N_kdf suchthat defined(x_O_1[ri], x_O[ri], r_2[ri]) && (x_O_3 = x_O_1[ri]) && (x_O_2 = x_O[ri]) then r_2[u] else r_3 <-R kdfkey; r_3)
     ))


Applying expand
  - Expand if/find/let
yields

Game 10 is
     Ostart() :=
     seed_1 <-R dh_keyseed;
     pk: dh_pkey <- dh_pkgen(seed_1);
     r_1 <-R dh_encapseed;
     s_1 <-R dh_secret;
     v_4: dh_output <- dh_encap_r(pk, r_1);
     c_1: dh_ciphertext <- 2-proj-dh_pair(v_4);
     return(pk, c_1);
     ((
       foreach iH <= N_kdf do
       OH(x1: bitstring, x2: dh_secret) :=
       x_O_1: bitstring <- x1;
       x_O: dh_secret <- x2;
       find [unique] u_1 = ri_1 <= N_kdf suchthat defined(x_O_1[ri_1], x_O[ri_1], r_2[ri_1]) && (x_O_1 = x_O_1[ri_1]) && (x_O = x_O[ri_1]) then
         return(r_2[u_1])
       orfind  suchthat defined(x_O_3, x_O_2, r_3) && (x_O_1 = x_O_3) && (x_O = x_O_2) then
         return(r_3)
       else
         r_2 <-R kdfkey;
         return(r_2)
     ) | (
       foreach id <= N_dec do
       ODecap(c: dh_ciphertext) :=
       if c = c_1 then
         yield
       else
         return(dh_decap(c, dh_skgen(seed_1)))
     ) | (
       OChal(ss_mlkem: bitstring) :=
       x_O_3: bitstring <- ss_mlkem;
       x_O_2: dh_secret <- s_1;
       find [unique] u = ri <= N_kdf suchthat defined(x_O_1[ri], x_O[ri], r_2[ri]) && (x_O_3 = x_O_1[ri]) && (x_O_2 = x_O[ri]) then
         K: kdfkey <- r_2[u]
       else
         r_3 <-R kdfkey;
         K: kdfkey <- r_3
     ))


Applying remove assignments of findcond
  - Remove assignments on x_O_3 (definition point kept, all usages removed)
  - Remove assignments on x_O_2 (definition removed, all usages removed)
  - Remove assignments on x_O_1 (definition point kept, all usages removed)
  - Remove assignments on x_O (definition removed, all usages removed)
yields

Game 11 is
     Ostart() :=
     seed_1 <-R dh_keyseed;
     pk: dh_pkey <- dh_pkgen(seed_1);
     r_1 <-R dh_encapseed;
     s_1 <-R dh_secret;
     v_4: dh_output <- dh_encap_r(pk, r_1);
     c_1: dh_ciphertext <- 2-proj-dh_pair(v_4);
     return(pk, c_1);
     ((
       foreach iH <= N_kdf do
       OH(x1: bitstring, x2: dh_secret) :=
       x_O_1: bitstring <- cst_bitstring;
       find [unique] u_1 = ri_1 <= N_kdf suchthat defined(x1[ri_1], x2[ri_1], x_O_1[ri_1], r_2[ri_1]) && (x1 = x1[ri_1]) && (x2 = x2[ri_1]) then
         return(r_2[u_1])
       orfind  suchthat defined(ss_mlkem, s_1, x_O_3, r_3) && (x1 = ss_mlkem) && (x2 = s_1) then
         return(r_3)
       else
         r_2 <-R kdfkey;
         return(r_2)
     ) | (
       foreach id <= N_dec do
       ODecap(c: dh_ciphertext) :=
       if c = c_1 then
         yield
       else
         return(dh_decap(c, dh_skgen(seed_1)))
     ) | (
       OChal(ss_mlkem: bitstring) :=
       x_O_3: bitstring <- cst_bitstring;
       find [unique] u = ri <= N_kdf suchthat defined(x1[ri], x2[ri], x_O_1[ri], r_2[ri]) && (ss_mlkem = x1[ri]) && (s_1 = x2[ri]) then
         K: kdfkey <- r_2[u]
       else
         r_3 <-R kdfkey;
         K: kdfkey <- r_3
     ))


Applying simplify [probability N_kdf / |dh_secret|]
  - Global dependency analysis on s_1
yields

Game 12 is
     Ostart() :=
     seed_1 <-R dh_keyseed;
     pk: dh_pkey <- dh_pkgen(seed_1);
     r_1 <-R dh_encapseed;
  {7}s_1 <-R dh_secret;
     v_4: dh_output <- dh_encap_r(pk, r_1);
     c_1: dh_ciphertext <- 2-proj-dh_pair(v_4);
     return(pk, c_1);
     ((
       foreach iH <= N_kdf do
       OH(x1: bitstring, x2: dh_secret) :=
       x_O_1: bitstring <- cst_bitstring;
 {24}  find [unique] u_1 = ri_1 <= N_kdf suchthat defined(x1[ri_1], x2[ri_1], x_O_1[ri_1], r_2[ri_1]) && (x1 = x1[ri_1]) && (x2 = x2[ri_1]) then
         return(r_2[u_1])
       else
         r_2 <-R kdfkey;
         return(r_2)
     ) | (
       foreach id <= N_dec do
       ODecap(c: dh_ciphertext) :=
       if c = c_1 then
         yield
       else
         return(dh_decap(c, dh_skgen(seed_1)))
     ) | (
       OChal(ss_mlkem: bitstring) :=
       x_O_3: bitstring <- cst_bitstring;
 {69}  find [unique] 
       else
         r_3 <-R kdfkey;
         K: kdfkey <- r_3
     ))


Applying simplify
  - Simplification pass
    - Find at 69 removed (else branch kept if any)
    - Replaced defined condition x1[ri_1], x2[ri_1], x_O_1[ri_1], r_2[ri_1] with r_2[ri_1], x2[ri_1], x1[ri_1] in find at 24
    - Remove random number generation at 7
yields

Game 13 is
     Ostart() :=
     seed_1 <-R dh_keyseed;
     pk: dh_pkey <- dh_pkgen(seed_1);
     r_1 <-R dh_encapseed;
     v_4: dh_output <- dh_encap_r(pk, r_1);
     c_1: dh_ciphertext <- 2-proj-dh_pair(v_4);
     return(pk, c_1);
     ((
       foreach iH <= N_kdf do
       OH(x1: bitstring, x2: dh_secret) :=
       x_O_1: bitstring <- cst_bitstring;
       find [unique] u_1 = ri_1 <= N_kdf suchthat defined(r_2[ri_1], x2[ri_1], x1[ri_1]) && (x1 = x1[ri_1]) && (x2 = x2[ri_1]) then
         return(r_2[u_1])
       else
         r_2 <-R kdfkey;
         return(r_2)
     ) | (
       foreach id <= N_dec do
       ODecap(c: dh_ciphertext) :=
       if c = c_1 then
         yield
       else
         return(dh_decap(c, dh_skgen(seed_1)))
     ) | (
       OChal(ss_mlkem: bitstring) :=
       x_O_3: bitstring <- cst_bitstring;
       r_3 <-R kdfkey;
       K: kdfkey <- r_3
     ))


Applying move all binders
  - Move assignment to x_O_1
yields

Game 14 is
     Ostart() :=
     seed_1 <-R dh_keyseed;
     pk: dh_pkey <- dh_pkgen(seed_1);
     r_1 <-R dh_encapseed;
     v_4: dh_output <- dh_encap_r(pk, r_1);
     c_1: dh_ciphertext <- 2-proj-dh_pair(v_4);
     return(pk, c_1);
     ((
       foreach iH <= N_kdf do
       OH(x1: bitstring, x2: dh_secret) :=
       find [unique] u_1 = ri_1 <= N_kdf suchthat defined(r_2[ri_1], x2[ri_1], x1[ri_1]) && (x1 = x1[ri_1]) && (x2 = x2[ri_1]) then
         return(r_2[u_1])
       else
         r_2 <-R kdfkey;
         return(r_2)
     ) | (
       foreach id <= N_dec do
       ODecap(c: dh_ciphertext) :=
       if c = c_1 then
         yield
       else
         return(dh_decap(c, dh_skgen(seed_1)))
     ) | (
       OChal(ss_mlkem: bitstring) :=
       x_O_3: bitstring <- cst_bitstring;
       r_3 <-R kdfkey;
       K: kdfkey <- r_3
     ))


Applying SA rename new without array accesses and remove assignments of findcond
  - Remove assignments on x_O_3 (definition removed, all usages removed)
yields

Game 15 is
     Ostart() :=
     seed_1 <-R dh_keyseed;
     pk: dh_pkey <- dh_pkgen(seed_1);
     r_1 <-R dh_encapseed;
     v_4: dh_output <- dh_encap_r(pk, r_1);
     c_1: dh_ciphertext <- 2-proj-dh_pair(v_4);
     return(pk, c_1);
     ((
       foreach iH <= N_kdf do
       OH(x1: bitstring, x2: dh_secret) :=
       find [unique] u_1 = ri_1 <= N_kdf suchthat defined(r_2[ri_1], x2[ri_1], x1[ri_1]) && (x1 = x1[ri_1]) && (x2 = x2[ri_1]) then
         return(r_2[u_1])
       else
         r_2 <-R kdfkey;
         return(r_2)
     ) | (
       foreach id <= N_dec do
       ODecap(c: dh_ciphertext) :=
       if c = c_1 then
         yield
       else
         return(dh_decap(c, dh_skgen(seed_1)))
     ) | (
       OChal(ss_mlkem: bitstring) :=
       r_3 <-R kdfkey;
       K: kdfkey <- r_3
     ))


Proved secrecy of K in game 15
Adv[Game 1: secrecy of K] <= 2 * N_kdf / |dh_secret| + 2 * P_dh_indcca2(time_1, 1, 1, N_dec) + Adv[Game 15: secrecy of K]
Adv[Game 15: secrecy of K] <= 0
RESULT Proved secrecy of K up to probability 2 * N_kdf / |dh_secret| + 2 * P_dh_indcca2(time_1, 1, 1, N_dec)
RESULT time_1 = time + time(context for game 2) = time(2-proj-dh_pair) + time(1-proj-dh_pair) + N_kdf * time(kdf, maxlength(game 2: x1)) + time(kdf, maxlength(game 2: ss_mlkem)) + time
All queries proved.
