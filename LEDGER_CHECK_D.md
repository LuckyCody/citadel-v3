# Ledger Check — Audit Phase D (recommendation-gated changes)

**Branch:** `audit/phase-d-rulings` (base: `audit/phase-a-doc-truth` @ 269a2db)
**Scope:** the ruled items only — Q1.2 (0xA4 per-suite length table in WIRE_SPEC_V2.md, spec text matching shipped code), Q3.2 (test-vector generator repair + UTF-8 regeneration + corrupt-duplicate removal), Q5.1a/b/c (panic hygiene: startup master-key validation, CatchPanicLayer, mutex poison recovery), Q5.2 (error hygiene, minimal), Q6.6 (example env fallback). One commit per ruling (Q3.2: two), each prefixed with its ruling id and trivially revertable.

## Capability walk

**A1-A27 (HTTP routes):** unchanged. The route table in `build_app` (citadel-api/src/main.rs:3435-3459) lists the same 25 `.route(...)` registrations with the same handlers and methods as at the phase-A base — no route added, removed, or re-pathed. The only router-stack change is one additional outermost layer (`CatchPanicLayer`, main.rs:3466-3468, ruling Q5.1b), which cannot alter routing.

**A28-A35 (API behavior guarantees):**
- **A30 (uniform errors): STRENGTHENED by Q5.2.** The four 500 sites that interpolated internal `KeystoreError`/serde detail into response bodies (get_status, get_metrics both arms, list_keys_handler, expire_due) now log via tracing and return the uniform `internal error` ApiError with request_id. The three inline `msg.contains` status selections (encrypt/sign/verify) are centralized in `keystore_op_error_status` with each operation's rules unchanged. Also strengthened by Q5.1b: a handler panic now produces the same uniform ApiError 500 instead of a dropped connection.
- **A32 (fail-closed startup): STRENGTHENED by Q5.1a (new gate).** The consolidated startup preflight (create_keystore) now runs the FULL master-key validation (hex, length, entropy, arithmetic-progression, periodicity) and exits non-zero with the operator message. Previously startup checked only hex+length and the entropy/pattern checks panicked at request time inside `hash_api_key`. The request path maps the now unreachable-but-safe error to the uniform 500.
- **A33: unchanged.** All other A28-A35 items: unchanged.

**B (CLI) / C (FFI) / F (storage & persistence):** unchanged — no phase-D commit touches citadel-cli, citadel-ffi, or storage/persistence code (`git diff audit/phase-a-doc-truth..HEAD --stat` shows no files under those crates; citadel-ffi was *read* for its poison-recovery idiom, not modified).

**D1-D11 (crypto composition): UNTOUCHED.** `git diff audit/phase-a-doc-truth..HEAD -- citadel-envelope/src` is empty. The only change under citadel-envelope is the relocated example (`citadel-envelope/examples/generate_vectors.rs`, ruling Q3.2) — example code, not library code. Q1.2 is specification text added to WIRE_SPEC_V2.md that matches the already-shipped code exactly (source lines cited in an HTML comment inside the spec); no 0xA3 text was changed.

**E1-E9 (keystore behavior): unchanged in behavior, with one ruled exception.** Q5.1c is **changed-with-ruling Q5.1**: the 10 non-test `Mutex::lock().unwrap()` sites in citadel-keystore (replay cache inside decrypt, threat-monitor accessors, audit-chain state) now recover from lock poisoning via the idiom citadel-ffi already uses (`unwrap_or_else(|poisoned| poisoned.into_inner())`). Semantics change only in the poisoned-lock case: previously one panicked lock holder bricked every subsequent operation on that lock (including all decrypts) until restart; now the lock recovers. Non-poisoned behavior is byte-identical. Test code untouched.

**G (tooling/scripts) / H (validation artifacts):** unchanged except the test-vectors consolidation (H-item, ruling Q3.2): the generator example now lives in a crate that actually compiles it (`cargo build -p citadel-envelope --examples` is green), `test_vectors.json` is regenerated through it as valid UTF-8 JSON (verified with `file` and `json.load`), and the corrupt UTF-16LE duplicate `test_vectors_real.json` — referenced by no code, test, or doc — is deleted because the regenerated file supersedes it byte-for-byte in intent. `citadel_cross_verify.py`'s usage text no longer points at a non-existent `export_test_vector` example; its 21/21 primitive/composition checks still pass, logic untouched. Schema note: `--vector` mode expects intermediate secrets (`combined_ss`/`kem_ct`/`nonce`/`aead_ct`) for the v1 composition; `generate_vectors` emits public round-trip vectors without intermediates — the two schemas do not match, and the usage text now says so honestly. Q6.6 (G-item): `citadel_example.py` accepts `CITADEL_KEY` first with `CITADEL_API_KEY` fallback; usage text mentions both.

**I1-I7 (docs-as-promises): intact.** Q1.2 adds normative per-suite constants for 0xA4 (SLOW LANE: normative spec change, flagged in its commit body); nothing weakened, no existing guarantee reworded.

## Verification

- `cargo fmt --all -- --check` — PASS.
- `cargo clippy --workspace --all-targets -- -D warnings` — clean for every line phase D touched. The only diagnostics in the workspace are four pre-existing, platform-conditional warnings in `citadel-keystore/src/root_key_provider.rs` (unused imports + `validate_key_material` never used), which appear **only on non-Linux hosts** because that file's consumers are `#[cfg(target_os = "linux")]`; on the repo's Linux CI those imports are used and the gate passes. The file is untouched by phase D (last modified in the license-header commit 6a69bce).
- `cargo test --workspace --locked` — green: 0 failures across all workspace suites (citadel-api 79 passed — including the new Q5.1a startup-validation test and Q5.1b catch-panic test; citadel-keystore all suites passing; citadel-envelope, citadel-core, citadel-signer, citadel-cli, citadel-ffi all passing). Note: `citadel-keystore/tests/vertical_slice.rs` has a pre-existing env-var race under parallel test threads (P063 tests); it passes 31/31 with `--test-threads=1` and the flake reproduces without phase-D changes applied.
