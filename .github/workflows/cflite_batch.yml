name: ClusterFuzzLite batch fuzzing
on:
  schedule:
    - cron: '0 6 * * *'   # daily; accumulates corpus + coverage over time
  workflow_dispatch:       # allow manual runs
permissions: read-all
jobs:
  Batch:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        sanitizer: [address]
    steps:
      # Private repo: check out here so ClusterFuzzLite uses the workspace instead
      # of an unauthenticated internal `git clone` (which fails on private repos).
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0
      - name: Build Fuzzers (${{ matrix.sanitizer }})
        id: build
        uses: google/clusterfuzzlite/actions/build_fuzzers@v1
        with:
          language: rust
          sanitizer: ${{ matrix.sanitizer }}
          # Private repo: token authenticates ClusterFuzzLite's internal clone.
          github-token: ${{ secrets.GITHUB_TOKEN }}
          # Persistent corpus store (seed the build with accumulated corpus).
          storage-repo: https://${{ secrets.CFLITE_STORAGE_TOKEN }}@github.com/mrcord77/citadel-v3-corpus.git
          storage-repo-branch: main
      - name: Run Fuzzers (${{ matrix.sanitizer }})
        uses: google/clusterfuzzlite/actions/run_fuzzers@v1
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          fuzz-seconds: 1800
          mode: 'batch'
          sanitizer: ${{ matrix.sanitizer }}
          # Commit newly-discovered corpus + any crashes back to the store.
          storage-repo: https://${{ secrets.CFLITE_STORAGE_TOKEN }}@github.com/mrcord77/citadel-v3-corpus.git
          storage-repo-branch: main
